import { BigNumberish } from 'ethers';
import { MerkleTree } from "./merkletree/index.js";
export type ToLeaf<T> = (element: T) => string;
export type Proof = string[];
export declare class MerkleTreeGenerator<T> {
    private elements;
    private toLeaf;
    private tree;
    constructor(elements: T[], toLeaf: ToLeaf<T>);
    generateTree(): MerkleTree;
    generateRoot(): string;
    generateProof(element: T): Proof;
    verifyProof(element: T, proof: Proof): boolean;
}
export type SaleItemsElement = {
    address: string;
    tokenId: BigNumberish;
};
export declare const getSaleItemsLeaf: ToLeaf<SaleItemsElement>;
