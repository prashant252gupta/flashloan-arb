export declare class PromiseCache {
    private readonly cache;
    constructor();
    do<S extends Array<unknown>, T>(key: string, validMilliseconds: number | undefined, task: (...args: S) => Promise<T>, ...args: S): Promise<T>;
}
