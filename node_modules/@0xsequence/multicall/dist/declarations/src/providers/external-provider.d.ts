import { providers } from 'ethers';
import { Multicall, MulticallOptions } from "../multicall.js";
type ExternalProvider = providers.ExternalProvider;
export declare class MulticallExternalProvider implements ExternalProvider {
    private provider;
    private multicall;
    constructor(provider: providers.ExternalProvider, multicall?: Multicall | Partial<MulticallOptions>);
    get isMetaMask(): boolean | undefined;
    get isStatus(): boolean | undefined;
}
export {};
